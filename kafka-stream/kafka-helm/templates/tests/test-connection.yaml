apiVersion: v1
kind: Pod
metadata:
  name: test-connection
  labels:
    app: {{ .Release.Name }}
spec:
  containers:
    - name: test-connection
      image: busybox
      command: ['sh', '-c', 'nc -zv kafka {{ .Values.kafka.service.port }}']
